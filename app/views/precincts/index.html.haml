%h1 Listing precincts

%table.table.table-striped
  %tr
    %th Number
    %th Location
    %th Address
    %th
    - if user_signed_in?
      %th
      %th

  - @precincts.each do |precinct|
    %tr
      %td= precinct.number
      %td= precinct.description
      %td= precinct.location
      %td= link_to 'Show', precinct
      - if user_signed_in?
        %td= link_to 'Edit', edit_precinct_path(precinct)
        %td= link_to 'Destroy', precinct, :confirm => 'Are you sure?', :method => :delete

%br
- if user_signed_in?
  = link_to 'New Precinct', new_precinct_path
